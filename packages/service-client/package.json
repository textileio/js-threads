{
  "name": "@textile/threads-service-client",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "@improbable-eng/grpc-web": "^0.12.0",
    "@textile/threads-core": "0.0.1",
    "@textile/threads-service-grpc": "^0.1.3"
  },
  "devDependencies": {
    "@types/randombytes": "^2.0.0",
    "cids": "github:multiformats/js-cid#master",
    "isomorphic-ws": "^4.0.1",
    "peer-id": "^0.13.6",
    "randombytes": "^2.1.0",
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  }
}
