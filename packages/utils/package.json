{
  "name": "@textile/threads-utils",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "@textile/threads-core": "0.0.1",
    "cbor-sync": "^1.0.4",
    "loglevel": "^1.6.6",
    "multiaddr": "^7.2.1",
    "multicodec": "0.5.6"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "typescript": "^3.7.2"
  }
}
